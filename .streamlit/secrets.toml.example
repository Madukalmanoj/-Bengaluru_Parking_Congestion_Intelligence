# Example secrets file for Streamlit Cloud
# Copy this to secrets.toml and fill in your values
# DO NOT commit secrets.toml to git!

# CSV data path (if needed)
CSV_PATH = "/mount/data/violations.csv"
